// Configuration for the app
// AI chat is now handled via GraphQL agent API

export const MAX_CONVERSATION_TOKENS = 4000;

// Chat configuration (used for display purposes)
export const CHAT_CONFIG = {
  model: 'agent',
  temperature: 0.7,
  max_tokens: 1000,
};
